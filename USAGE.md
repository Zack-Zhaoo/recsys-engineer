# 使用说明

装好之后直接用自然语言提问,不需要记命令,也不需要指定读哪个文件。

公共知识截止 2026-08-01,不联网。

## 怎么问

照下面这样说就行,不用加前缀或关键词。

**学习和理解**

- 我想从零系统学推荐,帮我规划路径
- 双塔召回的负采样到底在解决什么问题
- 粗排和精排的一致性为什么难保证

**设计和排查**

- 帮我设计一个短视频冷启动的召回方案
- 上线后 CTR 涨了但人均时长跌了,从哪查
- 多路召回的配额该按什么分

**判断和比较**

- LRM、生成式推荐、Agentic 到底差在哪
- 帮我分析这篇论文,值不值得跟进
- 快手在生成式推荐上公开过什么

**记录和复盘**

- 记下这个想法,用不确定性分配多样性预算
- 这个实验指标没涨,该迭代还是该停

后两组会走结构化的流程。论文分析会分开给出结论可信度、启发价值和场景迁移性,三个分别评,不合成一个总分。实验复盘必须写出新增认知、下一轮最小改动和停止条件,不会只按指标好坏二分。

## 回答遵守的规则

每个结论会区分证据级别,分成稳定共识、论文报告、公司报告、综合判断、作者观点、开放假设六种。涉及工业收益时用"论文/公司报告称"的措辞,公司自述不会被写成已验证的事实。二手解读只作导读,不能单独支撑部署、收益或"首次"这类声明。

深度会跟着提问者调整。表述像新手时,按"问题 → 直觉 → 最小例子 → 模块位置 → 指标 → 下一步"讲;表述像资深工程师时,直接进入约束、工业证据、替代方案和剩余的不确定性。也可以直接要求"讲深一点"或"我不熟这块,从头讲"。

问"最新进展"时会先说明这是 2026-08-01 的快照,不会把截止日之后的内容当成内置知识。如果运行环境有联网工具,可以要求额外核验,结果会标成快照外信息,但不会自动写进知识库。

`perspective/` 里的内容会明确标成作者判断,不和公开证据混写。要求中立综述时,正文只给公共证据。

## 你自己的区域

`workspace/` 存使用者的画像、能力评估、稳定观点和原创想法。不外传,版本更新也不会覆盖。

说"记下这个想法"就会记录并回显摘要。对话中出现稳定的能力证据或高价值假设时也可能记录,但会在回答末尾说明改了什么。记录原创想法时保留原始表达、归属、尚未验证的新颖性判断和已知反例。

自己查看或修改。

```bash
python3 scripts/manage_workspace.py status
python3 scripts/manage_workspace.py --help
```

## 边界

它不联网,也不覆盖 2026-08-01 之后的进展。这是设计意图,不是缺陷。

它给的是方案、诊断顺序和判据。实验要自己跑,数据要自己接,它也不提供可直接训练的模型实现。

结论会守住场景边界。离线增益不会被写成线上收益;反馈密度、物料生命周期或流量规模差得远时,会主动说明这个结论未必迁得过去。记录想法时同样不替人夸大原创性。

## 脚本

都在 `scripts/` 下,只读或只写 `workspace/`,不联网。

`validate_skill.py` 校验完整性,包括目录结构、内部链接、引用编号与来源目录的对应关系、许可文件。
`manage_workspace.py` 读写画像、能力、观点、想法。
`upgrade.py` 升级到新版本并保留 `workspace/`。
`refresh_snapshot.py` 推进知识截止日,并审计内容是否跟得上。
`build_skill_package.py` 打包,校验不通过不出包。

## 许可

内容 CC BY 4.0,`scripts/` MIT。可以使用、修改、商用、再分发,保留署名即可。细则见 [LICENSE.md](LICENSE.md)。
